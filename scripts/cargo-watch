#!/usr/bin/env badash
# A shim so I don't have to add cargo-watch to my path

# see https://github.com/passcod/cargo-watch for arguments here
# I almost always want to run the tests

command "$HOME/src/cargo-watch/target/debug/cargo-watch" "watch" "-x" "test"
# uncomment this to run arbitrary commands
#command "$HOME/src/cargo-watch/target/debug/cargo-watch" "watch" "$@"

