#!/usr/bin/env bash
# Kill a bunch of things to reset at the beginning of the day, or whenever

# All of these use -I, which asks for confirmation before killing

COLOR_FG_BOLD_BLUE='\033[1;34m'
COLOR_RESET='\033[0m'

# echo with blue text
echo_info() {
  echo -e "${COLOR_FG_BOLD_BLUE}$@${COLOR_RESET}"
}

# try to kill the input process
kill_proc_name() {
  # arguments
  proc_name="$1"

  echo_info "killing '$proc_name'"
  pkill -I "$proc_name" || echo "(no process found)"
}

# close the Cisco AnyConnect VPN client, if I had that running at home
kill_proc_name "AnyConnect"

# close Outlook, so I can restart it to pick up any new stuff
# (this will bring up a dialog about force quitting, whatever I just want to close you dumb program)
kill_proc_name "Outlook"

# close Slack, to reclaim memory and reset its state
kill_proc_name "Slack"

# same with Discord
kill_proc_name "Discord"

# in case I left this open
kill_proc_name "KeePassX"

# any images, PDFs, etc. that are still open
kill_proc_name "Preview"

# if I left Cisco Proximity running
# (lower case b/c the proc name is that way)
kill_proc_name "proximity"

# if I left VLC running after listening to music
kill_proc_name "VLC"
