#!/usr/bin/env bash
###########################################################################
# DO NOT EDIT! This script was auto-generated. To update this script, edit
# the file script-gen/new_chrome, and run './generate-scripts.sh'
###########################################################################
#e6a595edf280dd624989afbec32d2297

COLOR_FG_RED='\033[0;31m'
COLOR_RESET='\033[0m'
echo_err ()
{
    echo -e "${COLOR_FG_RED}$@${COLOR_RESET}" 1>&2
}
requirement_check ()
{
    local cmd="$1";
    local how_to_install="$2";
    if [ ! $(command -v $cmd) ]; then
        echo_err "[ERROR] Command '$cmd' is required for this script, but not installed";
        echo_err "To install: $how_to_install";
        return 1;
    else
        return 0;
    fi
}
combined_return=0
requirement_check osascript "search 'how to install osascript'"
combined_return=$(( combined_return + $? ))
requirement_check pbpaste "search 'how to install pbpaste'"
combined_return=$(( combined_return + $? ))
if [ "$combined_return" != 0 ]; then exit $combined_return; fi
clipboard_contents="$(pbpaste)"
if [[ "$clipboard_contents" =~ ^http ]]
then
  osascript <<END_SCRIPT
tell application "Google Chrome"
  make new window
  set URL of active tab of window 1 to "$clipboard_contents"
  activate
end tell
END_SCRIPT
else
  osascript -e 'tell application "Google Chrome" to make new window activate'
fi
