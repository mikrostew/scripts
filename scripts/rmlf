#!/usr/bin/env bash
###########################################################################
# DO NOT EDIT! This script was auto-generated. To update this script, edit
# the file script-gen/rmlf, and run './generate-scripts.sh'
###########################################################################
#b5c3aa5a4340abe86de33778e86d9a23

COLOR_FG_RED='\033[0;31m'
COLOR_RESET='\033[0m'
echo_err ()
{
    echo -e "${COLOR_FG_RED}$@${COLOR_RESET}" 1>&2
}
num_arguments_ok ()
{
    local expected_args=("${!1}");
    local num_args="$2";
    local expected_num_args="${#expected_args[@]}";
    if [ "$expected_num_args" -ne "$num_args" ]; then
        echo_err "Come on! This is how to use this:";
        echo_err "";
        echo_err "  ${FUNCNAME[1]} ${expected_args[*]}";
        return -1;
    fi;
    return 0
}
arguments=('<filename>')
if num_arguments_ok arguments[@] "$#"
then
  printf %s "$(< $1)" > $1
fi
